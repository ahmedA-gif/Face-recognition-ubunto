# Core runtime
numpy==1.26.4
opencv-python==4.11.0.86
Pillow==11.3.0
PyYAML==6.0.2

# Detection and recognition
ultralytics==8.4.112
onnxruntime==1.18.1
insightface==0.7.3

# FAISS-CPU — explicit CPU-only vector search for the face gallery
# Install ONLY faiss-cpu (NOT faiss-gpu) on CPU machines.
# Incompatible builds will be warned about and silently downgraded to NumPy.
faiss-cpu==1.8.0

# Tracking
scipy==1.13.1
filterpy==1.4.5
lapx==0.5.2

# Real-time event stream (attendance:events) — replaces CSV on the hot path
redis==5.2.1

# Optional (notebooks / preview API):
# jupyter
# ipywidgets
# tqdm
# requests
# fastapi
# uvicorn
# psycopg2-binary   # attendance worker -> PostgreSQL (Phase 6)
